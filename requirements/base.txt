# Core scientific + orchestration stack (framework-agnostic)
numpy>=1.26,<2.1
pandas>=2.1
tqdm>=4.66
pyyaml>=6.0
python-dotenv>=1.0
hydra-core>=1.3
omegaconf>=2.3
rich>=13.7

# Experiment tracking (both installed, both usable)
wandb>=0.17
mlflow>=2.14

# Notebooks
jupyter>=1.0
jupyterlab>=4.2
ipykernel>=6.29
